{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create new category</h1>

    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.code) }}
        {{ form_row(form.save) }}

       
    {{ form_end(form) }}


    <a href="{{ path('product_index') }}">Go back to the list</a>
{% endblock %}
