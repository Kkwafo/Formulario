{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create with category product</h1>

    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.code) }}
        {{ form_row(form.save) }}
    {{ form_end(form) }}

    <a href="{{ path('product_index') }}">Go back to list</a>
{% endblock %}
